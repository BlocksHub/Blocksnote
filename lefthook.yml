pre-push:
  jobs:
    - run: bun test
      skip: false

pre-commit:
  parallel: true
  jobs:
    - run: bun run eslint --fix "{staged_files}"
      glob:
        - "*.ts"
        - "*.js"
        - "*.tsx"
        - "*.jsx"
      stage_fixed: true